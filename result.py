class Result:
    def __init__(self, size, algorithm, sort_type, duration, iterations):
        self.size = size
        self.algorithm = algorithm
        self.sort_type = sort_type
        self.duration = duration
        self.iterations = iterations
